<%= form_for @beer do |f| %>
	<%= f.label :Nimi %>
	<%= f.text_field :name, id:'name' %>
	<%= f.label :Tyyppi %>
	<%= f.text_field :beer_type, id:'beer_type' %>
	<%= f.label :Alc %>
	<%= f.text_field :alc, id:'alc'%>
	<%= f.label :Valmistusmaa %>
	<%= f.text_field :country, id:'country' %>
	<%= f.label :Panimo %>
	<%= f.text_field :brewery, id:'brewery' %>
	<% if @beer.name == nil %>
		<%= submit_tag "Lisää olut", id:"Lisaa olut" %>
	<%else%>
		<%= submit_tag "Päivitä olut", id:"Paivita olut" %>
	<% end %>
<% end %>